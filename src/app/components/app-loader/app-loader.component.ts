// Import necessary Angular modules and dependencies
import { Component, OnInit } from '@angular/core';

@Component({
  selector: 'app-loader', // HTML selector for using this component
  templateUrl: './app-loader.component.html', // Path to the component's HTML template file
  styleUrls: ['./app-loader.component.scss'], // Path to the component's CSS styles file
})
// Define the component class
export class LoaderComponent implements OnInit {
  constructor() {
  }
  ngOnInit(): void {

  }
}
